name: Integration Tests

on:
  push:
    branches: [main, development]
  pull_request:
    branches: [main, development]

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build Docker image
        run: docker build -t renderer-test .

      - name: Start container
        run: |
          docker run -d \
            --name renderer-test \
            -p 3000:3000 \
            -e MOJO_MODE=development \
            -e problemJWTsecret=test-problem-secret \
            -e webworkJWTsecret=test-session-secret \
            -v "${{ github.workspace }}/t/ci/fixtures:/usr/app/private/test:ro" \
            renderer-test \
            morbo -l 'http://*:3000' ./script/renderer

      - name: Wait for health
        run: |
          for i in $(seq 1 30); do
            if curl -sf --max-time 5 http://localhost:3000/health >/dev/null 2>&1; then
              echo "Renderer healthy after $((i * 2))s"
              exit 0
            fi
            sleep 2
          done
          echo "Renderer failed to start"
          docker logs renderer-test 2>&1 | tail -50
          exit 1

      - name: PG unit tests (informational)
        continue-on-error: true
        run: |
          docker exec renderer-test bash -c \
            'export PG_ROOT=/usr/app/lib/PG && cd $PG_ROOT && prove -lr \
              t/macros t/contexts t/math_objects t/pg_problems t/units'

      - name: Renderer Perl tests (t/*.t)
        run: docker exec renderer-test bash -c 'cd /usr/app && prove -lr t/'

      # Bash HTTP suites — informational until WW3-R53. Resurrected this pass
      # (script name, secrets, set -e counter gotcha); they run but a few
      # assertions have drifted from the current API. The Perl t/*.t step above
      # is the gating coverage; re-gate these once WW3-R53 triages the drift.
      - name: Smoke tests
        continue-on-error: true
        run: bash t/ci/01-smoke.sh

      - name: Endpoint tests
        continue-on-error: true
        run: bash t/ci/04-endpoints.sh

      - name: Cleanup
        if: always()
        run: |
          docker stop renderer-test 2>/dev/null || true
          docker rm renderer-test 2>/dev/null || true
