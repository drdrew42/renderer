*.DS_Store
renderer.conf
lib/.pls-tmp-*
webwork-open-problem-library
private
tmp/*
!tmp/.gitkeep
logs/*

node_modules
public/**/*.min.js
public/**/*.min.css
public/static-assets.json

*.o
*.pm.tdy
*.bs
*.pid
.idea/

# Local-only Claude context (machine paths, deployment hosts). The shared
# CLAUDE.md and t/CLAUDE.md are tracked — general, public-safe context; the
# .local.md holds the machine/deployment particulars and stays untracked.
CLAUDE.local.md
